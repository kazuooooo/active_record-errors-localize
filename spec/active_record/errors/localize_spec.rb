RSpec.describe ActiveRecord::Errors::Localize do
  it "has a version number" do
    expect(ActiveRecord::Errors::Localize::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
